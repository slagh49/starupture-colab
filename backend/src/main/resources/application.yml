spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://postgres:5432/starrupture?reWriteBatchedInserts=true}
    username: ${DB_USER:scanner}
    password: ${DB_PASSWORD:scanner}
  jpa:
    hibernate.ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        jdbc:
          batch_size: 100
        order_inserts: true
        order_updates: true
  flyway:
    enabled: true
  data.redis:
    host: ${REDIS_HOST:redis}
    port: 6379
  servlet.multipart:
    max-file-size: 50MB
    max-request-size: 50MB

app:
  auth:
    # HMAC signing secret for auth tokens. Override in prod via APP_AUTH_SECRET.
    secret: ${APP_AUTH_SECRET:starrupture-homelab-default-hmac-secret-change-me-9f3a2b7c1d}
    token-ttl-hours: 168
    # Default admin seeded on first start if no users exist.
    default-admin-user: ${APP_ADMIN_USER:admin}
    default-admin-password: ${APP_ADMIN_PASSWORD:admin}

server:
  port: 8080

logging:
  level:
    com.starrupture: DEBUG
