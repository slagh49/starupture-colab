spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/starrupture
    username: scanner
    password: scanner
  jpa:
    show-sql: true
  data.redis:
    host: localhost
    port: 6379

logging:
  level:
    com.starrupture: TRACE
    org.hibernate.SQL: DEBUG
