services:
  nginx:
    ports:
      - "8081:80"
    # Pas de TLS en staging

  backend:
    environment:
      SPRING_PROFILES_ACTIVE: staging

  postgres:
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD:-staging_password}
