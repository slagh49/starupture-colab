import styles from './ProductionTable.module.css';

// Sprint 2: Sortable, filterable, searchable production table
export function ProductionTable(): JSX.Element {
  return (
    <div className={styles.container}>
      <p className={styles.placeholder}>Production table — Sprint 2</p>
    </div>
  );
}
