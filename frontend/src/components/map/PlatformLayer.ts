import { world2screen } from '../../constants/mapConfig';

export function drawPlatforms(
  _ctx: CanvasRenderingContext2D,
  _zoom: number,
  _panX: number,
  _panY: number,
  _canvasWidth: number,
  _canvasHeight: number
): void {
  // Sprint 2 implementation: draw platform grid as small orange squares
  // Will use world2screen for coordinate conversion
  void world2screen;
}
